# в папке DB ER диаграмма и дамп базы
# в папке design дизайны страниц
# проект делал через OSPanel, в моей версии при подключении к базе нужно указывать версию SQL, в более старых нужно указывать локалхост на сколько знаю
# файл подключение к базе в папке includes файл db
# так же отредактировать файл osp/project.ini
